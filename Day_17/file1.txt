# git initialization - empty git repository is crated
git init 

# files add to git
git add file_name     :[only single file is added]
git add .             :[all files inside this folder are added]

# commit to git 
git commit -m "meaning full messages"

# git log   :show all the commits 

----------------today-----------------------
https://www.toptal.com/developers/gitignore/api/python

git status  

git branch new_branch

git checkout new_branch

git merge new_branch

git branch -M main : to rename the branch to main

git push origin new_branch : to add new brach everytime



git remote add origin <git repo url> : connect remote reop to local repo 

git remote --v : to see remote repo

git push -u origin main : to push the main branch to website for the first push 

git push: for furthr push 

